version: "2"

linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - gosec
    - govet
    - ineffassign
    - misspell
    - revive
    - sqlclosecheck
    - staticcheck
  exclusions:
    paths:
      - tmp
      - bin

formatters:
  exclusions:
    paths:
      - tmp
      - bin
